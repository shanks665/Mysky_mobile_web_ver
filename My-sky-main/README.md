# Meetify

<<<<<<< HEAD
<div align="center">

**位置情報ベースのソーシャルネットワーキングアプリ**

近くのイベントやサークルを探し、新しい出会いを提供するモバイルアプリケーション

[![React Native](https://img.shields.io/badge/React%20Native-0.78.1-blue.svg)](https://reactnative.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0.4-blue)](https://www.typescriptlang.org/)
[![Firebase](https://img.shields.io/badge/Firebase-latest-orange)](https://firebase.google.com/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

</div>

---

## 📋 目次

- [概要](#概要)
- [主要機能](#主要機能)
- [技術スタック](#技術スタック)
- [プロジェクト構造](#プロジェクト構造)
- [セットアップ](#セットアップ)
- [開発](#開発)
- [テスト](#テスト)
- [ビルドとデプロイ](#ビルドとデプロイ)
- [CI/CD](#cicd-パイプライン)
- [アーキテクチャ](#アーキテクチャ)
- [コントリビューション](#コントリビューション)
- [ライセンス](#ライセンス)

---

## 🎯 概要

Meetifyは、位置情報を活用したソーシャルネットワーキングアプリケーションです。ユーザーは自分の興味に合ったサークルやイベントを見つけ、参加することで、新しい人々との出会いを実現できます。

### 🌟 主要機能

#### 🔐 認証・ユーザー管理
- **Firebase Authentication** による安全なユーザー認証
- メール/パスワード認証
- プロフィール管理（ニックネーム、プロフィール写真、カバー写真、自己紹介）
- フォロー/フォロワーシステム
- アカウントプライバシー設定（公開/非公開）
- フォローリクエスト機能（非公開アカウント用）
- ユーザーブロック機能

#### 📍 位置情報機能
- リアルタイム位置情報取得
- プライバシー保護のための位置情報難読化（3段階の精度設定）
- 近くのユーザー検索（半径指定可能）
- 距離計算（ハーバーサイン公式使用）
- 位置情報プライバシーレベル設定（公開/フォロワーのみ/非公開）

#### 👥 サークル機能
- サークルの作成・管理
- カテゴリー別サークル分類
- 公開/非公開サークル設定
- メンバー管理（管理者、メンバー、保留中、拒否）
- サークル専用掲示板
- サークル検索機能
- 最大メンバー数制限設定
- 地域・都道府県別フィルター

#### 📅 イベント機能
- イベントの作成・編集・削除
- 開催日時・場所の設定
- 複数写真のアップロード
- 参加ステータス管理（参加/検討中/不参加）
- 最大参加者数設定
- サークルとの関連付け（任意）
- イベント専用掲示板
- 参加者一覧表示
- 近くのイベント通知
- 開催予定イベントのリマインダー通知

#### 💬 ソーシャル機能
- **フィード**: SNS型の投稿機能
  - テキスト・画像投稿
  - いいね機能
  - コメント機能
  - 引用投稿
  - 返信機能
  - ハッシュタグ
  - メンション
  - 位置情報タグ
- **メッセージング**: 1対1のリアルタイムチャット
- **掲示板**: サークル・イベント専用の掲示板機能
  - スレッド型返信
  - 画像添付
  - いいね機能

#### 🔔 通知機能
- フォローリクエスト通知
- サークル参加リクエスト・承認・拒否通知
- イベント参加リクエスト・承認・拒否通知
- 近くで開催されるイベント通知
- 参加予定イベントのリマインダー
- バックグラウンド通知処理

#### 🔍 検索・発見機能
- ユーザー検索
- サークル検索
- イベント検索
- カテゴリー別フィルター
- 距離別ソート
- 都道府県別フィルター

---

## 🛠 技術スタック

### フロントエンド
- **React Native 0.78.1** - クロスプラットフォームモバイルアプリ開発
- **TypeScript 5.0.4** - 型安全な開発
- **React Navigation 7.x** - ナビゲーション管理
- **React Context API** - 状態管理

### バックエンド・サービス
- **Firebase Authentication** - ユーザー認証
- **Firebase Firestore** - NoSQLデータベース
- **Firebase Storage** - 画像・ファイルストレージ
- **Firebase App Distribution** - テストビルド配布

### 位置情報・地図
- **react-native-geolocation-service** - 位置情報取得
- **react-native-maps** - 地図表示
- **geolib** - 地理計算ライブラリ

### UI/UXライブラリ
- **react-native-vector-icons** - アイコン
- **react-native-image-picker** - 画像選択
- **@react-native-community/datetimepicker** - 日付・時刻選択
- **react-native-safe-area-context** - セーフエリア対応

### 開発ツール
- **ESLint** - コード品質チェック
- **Prettier** - コードフォーマッター
- **Jest** - ユニットテスト
- **React Test Renderer** - コンポーネントテスト
- **Husky** - Gitフック管理
- **lint-staged** - ステージングファイルのLint実行

### CI/CD
- **GitHub Actions** - 自動化ワークフロー
- **Fastlane** - ビルド・デプロイ自動化
- **Docker** - コンテナ化
- **SonarCloud** - コード品質分析

---

## 📁 プロジェクト構造

```
meetify/
├── android/                    # Androidネイティブコード
├── ios/                        # iOSネイティブコード
├── __tests__/                  # テストファイル
│   ├── models/                # モデルのテスト
│   ├── services/              # サービスのテスト
│   ├── utils/                 # ユーティリティのテスト
│   └── App.test.tsx           # アプリ全体のテスト
├── src/                        # ソースコード
│   ├── components/            # 再利用可能なコンポーネント
│   │   ├── PrefectureSelector.tsx    # 都道府県選択コンポーネント
│   │   ├── UserDistance.tsx          # ユーザー距離表示
│   │   └── modals/                   # モーダルコンポーネント
│   ├── constants/             # 定数定義
│   │   └── colors.ts          # カラー定数
│   ├── contexts/              # Reactコンテキスト
│   │   ├── AuthContext.tsx    # 認証コンテキスト
│   │   └── LocationContext.tsx # 位置情報コンテキスト
│   ├── models/                # データモデル・インターフェース
│   │   ├── User.ts            # ユーザーモデル
│   │   ├── Circle.ts          # サークルモデル
│   │   ├── Event.ts           # イベントモデル
│   │   ├── Post.ts            # 投稿モデル
│   │   ├── BoardPost.ts       # 掲示板投稿モデル
│   │   └── Notification.ts    # 通知モデル
│   ├── navigation/            # ナビゲーション設定
│   │   ├── AuthNavigator.tsx  # 認証画面ナビゲーション
│   │   ├── MainNavigator.tsx  # メイン画面ナビゲーション
│   │   ├── RootNavigator.tsx  # ルートナビゲーション
│   │   └── types.ts           # ナビゲーション型定義
│   ├── screens/               # 画面コンポーネント
│   │   ├── auth/              # 認証関連画面
│   │   ├── circles/           # サークル関連画面
│   │   ├── events/            # イベント関連画面
│   │   ├── feed/              # フィード関連画面
│   │   ├── messages/          # メッセージ関連画面
│   │   └── profile/           # プロフィール関連画面
│   ├── services/              # ビジネスロジック・API
│   │   ├── userService.ts     # ユーザー関連サービス
│   │   ├── locationService.ts # 位置情報サービス
│   │   ├── notificationService.ts # 通知サービス
│   │   ├── boardService.ts    # 掲示板サービス
│   │   └── backgroundService.ts # バックグラウンドサービス
│   ├── styles/                # グローバルスタイル
│   │   └── theme.ts           # テーマ設定
│   └── utils/                 # ユーティリティ関数
│       ├── distanceCalculator.ts # 距離計算
│       ├── locationUtils.ts   # 位置情報ユーティリティ
│       ├── prefectureData.ts  # 都道府県データ
│       └── defaultImages.ts   # デフォルト画像
├── .github/                    # GitHub設定
│   └── workflows/             # GitHub Actionsワークフロー
├── fastlane/                   # Fastlane設定
├── App.tsx                     # アプリエントリーポイント
├── index.js                    # React Nativeエントリーポイント
├── package.json                # 依存関係定義
├── tsconfig.json               # TypeScript設定
├── jest.config.js              # Jest設定
├── babel.config.js             # Babel設定
└── README.md                   # このファイル
```

---

## ⚡ クイックスタート

プロジェクトをすぐに始めるための最小限の手順：

```bash
# 1. リポジトリをクローン
git clone https://github.com/shanks665/My-sky.git
cd meetify

# 2. 依存関係をインストール
npm install

# 3. Metroバンドラーを起動
npm start

# 4. 別のターミナルでアプリを実行
npm run android  # Androidの場合
npm run ios      # iOSの場合（macOSのみ）

# 5. テストを実行
npm test
```

---

## 🚀 セットアップ

### 前提条件

- **Node.js** 18.x以上
- **npm** または **yarn**
- **React Native CLI**
- **Android Studio** (Android開発用)
- **Xcode** (iOS開発用、macOSのみ)
- **Firebase** プロジェクト

### インストール手順

1. **リポジトリのクローン**
   ```bash
   git clone https://github.com/shanks665/My-sky.git
   cd meetify
   ```

2. **依存関係のインストール**
   ```bash
   npm install
   # または
   yarn install
   ```

3. **iOS依存関係のインストール**（macOSのみ）
   ```bash
   cd ios
   pod install
   cd ..
   ```

4. **Firebase設定**
   - Firebase Consoleでプロジェクトを作成
   - `google-services.json` を `android/app/` に配置
   - `GoogleService-Info.plist` を `ios/` に配置
   - Firebase Authentication、Firestore、Storageを有効化

5. **環境変数の設定**
   - `.env` ファイルを作成（必要に応じて）
   ```env
   FIREBASE_API_KEY=your_api_key
   FIREBASE_PROJECT_ID=your_project_id
   ```

---

## 💻 開発

### 開発サーバーの起動

```bash
# Metroバンドラーを起動
npm start
# または
yarn start
```

### アプリの実行

#### Android
```bash
npm run android
# または
yarn android
```

#### iOS（macOSのみ）
```bash
npm run ios
# または
yarn ios
```

### コード品質チェック

```bash
# ESLintによるLint
npm run lint

# TypeScriptの型チェック
npm run tsc

# Prettierによるフォーマット
npm run format
```

---

## 🧪 テスト

### テストの実行

```bash
# すべてのテストを実行
npm test

# 特定のテストファイルを実行
npm test -- __tests__/App.test.tsx

# カバレッジレポート付きでテスト実行
npm run test:coverage
```

### テスト構成

- **ユニットテスト**: モデル、ユーティリティ、サービスのテスト
- **統合テスト**: コンポーネントとサービスの統合テスト
- **スナップショットテスト**: UIコンポーネントのレンダリングテスト

---

## 📦 ビルドとデプロイ

### ローカルビルド

#### Android
```bash
# リリースビルド
npm run build:android

# Fastlaneを使用したビルド
npm run fastlane:android:beta
```

#### iOS（macOSのみ）
```bash
# リリースビルド
npm run build:ios

# Fastlaneを使用したビルド
npm run fastlane:ios:beta
```

### Fastlaneによる自動デプロイ

Fastlaneを使用して、Firebase App Distributionへの配布を自動化できます：

```bash
# Android開発ビルドを配布
cd android
fastlane android distribute

# iOS開発ビルドを配布（macOSのみ）
cd ios
fastlane ios distribute
```

### Docker

CI/CD環境やローカル開発でDockerを使用できます：
=======
モバイルアプリケーション「Meetify」のリポジトリです。

## 概要

Meetifyは、近くのイベントやサークルを探し、新しい出会いを提供するモバイルアプリケーションです。ユーザーは自分の興味に合ったサークルやイベントを見つけ、参加することができます。

## 技術スタック

- React Native (TypeScript)
- Firebase
  - Authentication: ユーザー認証
  - Firestore: データベース
  - Storage: 画像・ファイル保存
  - Functions: サーバーサイドロジック
  - App Distribution: テスト配布

## プロジェクト構造

```
meetify/
├── android/              # Androidネイティブコード
├── ios/                  # iOSネイティブコード
├── src/                  # アプリケーションのソースコード
│   ├── components/       # 再利用可能なコンポーネント
│   ├── contexts/         # Reactコンテキスト
│   ├── models/           # データモデルとインターフェース
│   ├── navigation/       # ナビゲーション設定
│   ├── screens/          # 画面コンポーネント
│   ├── services/         # APIとサービス
│   ├── styles/           # グローバルスタイル
│   └── utils/            # ユーティリティ関数
├── .env                  # 環境変数
└── ...
```

## CI/CD パイプライン

このプロジェクトでは、継続的インテグレーション/継続的デリバリー (CI/CD) パイプラインを実装しています。

### GitHub Actions ワークフロー

以下のワークフローが設定されています：

1. **Pull Requestチェック** (pr-check.yml)
   - コード品質チェック (ESLint, TypeScript)
   - テスト実行
   - SonarCloudによるコード解析
   - 依存関係の脆弱性チェック
   - PRにチェック結果をコメント

2. **CI/CD パイプライン** (ci.yml)
   - 静的解析とテスト
   - Androidビルド
   - iOSビルド
   - Firebase App Distributionへのデプロイ
   - Slack通知

### ビルドとデプロイ

#### Fastlane

このプロジェクトではFastlaneを使用して、モバイルアプリのビルドとデプロイを自動化しています：

```bash
# Android開発ビルドをFirebaseに配布
npm run fastlane:android:beta

# iOS開発ビルドをFirebaseに配布
npm run fastlane:ios:beta
```

#### Docker

CI/CD環境用のDockerfileも用意されています：
>>>>>>> 357d4af7f6ed0fadfedca57e9d83f86e7e87ccd6

```bash
# Dockerイメージをビルド
docker build -t meetify .

# コンテナを実行
docker run -p 8081:8081 meetify
```

<<<<<<< HEAD
---

## 🔄 CI/CD パイプライン

### GitHub Actions ワークフロー

#### 1. Pull Requestチェック (`pr-check.yml`)
PRが作成されるたびに自動実行：
- ✅ コード品質チェック（ESLint）
- ✅ TypeScript型チェック
- ✅ ユニットテスト実行
- ✅ SonarCloudによるコード解析
- ✅ 依存関係の脆弱性チェック
- ✅ PRへのチェック結果コメント

#### 2. CI/CDパイプライン (`ci.yml`)
`develop`または`main`ブランチへのマージ時に実行：
- 🔨 Androidビルド
- 🔨 iOSビルド
- 📱 Firebase App Distributionへのデプロイ
- 💬 Slack通知

### SonarCloud統合

コード品質を継続的に監視：
- コードカバレッジ
- コード重複
- セキュリティホットスポット
- 技術的負債
- バグ・脆弱性の検出

結果は[SonarCloudダッシュボード](https://sonarcloud.io/dashboard?id=meetify-app)で確認できます。

---

## 🏗 アーキテクチャ

### データフロー

```
User Interface (Screens)
    ↓
React Context (Auth, Location)
    ↓
Services (userService, locationService, etc.)
    ↓
Firebase (Firestore, Storage, Auth)
```

### 状態管理

- **AuthContext**: ユーザー認証状態とユーザー情報の管理
- **LocationContext**: 位置情報の管理
- **Local State**: 画面固有の状態は`useState`で管理

### ナビゲーション構造

```
RootNavigator
├── AuthNavigator (未認証時)
│   ├── Login
│   ├── Register
│   └── ForgotPassword
└── MainNavigator (認証後)
    ├── HomeTab (フィード)
    ├── DiscoverTab (探索)
    ├── EventsTab (イベント)
    ├── MessagesTab (メッセージ)
    ├── NotificationsTab (通知)
    └── ProfileTab (プロフィール)
```

### データモデル

- **User**: ユーザー情報、フォロー関係、位置情報
- **Circle**: サークル情報、メンバー管理
- **Event**: イベント情報、参加者管理
- **Post**: フィード投稿、いいね、コメント
- **BoardPost**: 掲示板投稿、スレッド型返信
- **Notification**: 通知データ

---



### ブランチ戦略

- `main`: 本番環境用ブランチ
- `develop`: 開発環境用ブランチ
- `feature/*`: 機能開発用ブランチ
- `bugfix/*`: バグ修正用ブランチ
- `hotfix/*`: 緊急修正用ブランチ

### 開発フロー

1. `develop`ブランチから新しいブランチを作成
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/your-feature-name
   ```

2. 変更を実装し、コミット
   ```bash
   git add .
   git commit -m "feat: add your feature"
   ```

3. コード品質チェックとテストを実行
   ```bash
   npm run lint
   npm run tsc
   npm test
   ```

4. リモートにプッシュし、Pull Requestを作成
   ```bash
   git push origin feature/your-feature-name
   ```

5. レビューを受け、承認後に`develop`へマージ

### コミットメッセージ規約

[Conventional Commits](https://www.conventionalcommits.org/)に従います：

- `feat:` 新機能
- `fix:` バグ修正
- `docs:` ドキュメント変更
- `style:` コードスタイル変更（機能に影響なし）
- `refactor:` リファクタリング
- `test:` テスト追加・修正
- `chore:` ビルドプロセスや補助ツールの変更

---

## 🔧 トラブルシューティング

### TypeScriptエラー

**エラー**: `Cannot find type definition file for 'jest'` または `Cannot find type definition file for 'node'`

**解決方法**:
```bash
# 依存関係を再インストール
npm install
# または
yarn install

# node_modulesをクリーンアップしてから再インストール
rm -rf node_modules package-lock.json
npm install
```

**注意**: プロジェクトには`jest.d.ts`というグローバル型定義ファイルが含まれています。これは`@types/jest`がインストールされていない場合の代替として機能します。`npm install`を実行すると、正式な`@types/jest`パッケージがインストールされ、エラーが解消されます。

### Jestテストエラー

**エラー**: テストファイルで`jest`、`describe`、`it`、`expect`が認識されない

**解決方法**:
1. `@types/jest`がインストールされていることを確認
2. `tsconfig.json`に`"types": ["jest"]`が含まれていることを確認
3. IDEを再起動

### Metro Bundlerエラー

**エラー**: `Unable to resolve module`

**解決方法**:
```bash
# Metroキャッシュをクリア
npm start -- --reset-cache
# または
yarn start --reset-cache
```

### iOS Pod インストールエラー

**エラー**: CocoaPodsの依存関係エラー

**解決方法**:
```bash
cd ios
pod deintegrate
pod install
cd ..
```

### Androidビルドエラー

**エラー**: Gradleビルドの失敗

**解決方法**:
```bash
cd android
./gradlew clean
cd ..
npm run android
```

**エラー**: `Error resolving plugin [id: 'com.facebook.react.settings']`

**解決方法**:
1. `node_modules`を削除して再インストール:
   ```bash
   rm -rf node_modules
   npm install
   ```
2. Androidキャッシュをクリア:
   ```bash
   cd android
   ./gradlew clean
   ./gradlew cleanBuildCache
   cd ..
   ```
3. React Nativeキャッシュをクリア:
   ```bash
   npx react-native start --reset-cache
   ```

**エラー**: `hermesEnabled`や`enableHermes`が見つからない

**解決方法**: `android/gradle.properties`に以下を確認:
```properties
hermesEnabled=true
newArchEnabled=false
```

---


---

## 📧 お問い合わせ

質問やフィードバックがある場合は、以下の方法でお問い合わせください：

- **Issue**: [GitHub Issues](https://github.com/shanks665/My-sky/issues)

---

<div align="center">

**Made with ❤️ by the Meetify Team**

[⬆ トップへ戻る](#meetify)

</div>
=======
## SonarCloud解析

コード品質を維持するために、SonarCloudによる静的コード解析を導入しています。解析結果は[SonarCloudダッシュボード](https://sonarcloud.io/dashboard?id=meetify-app)で確認できます。

## 環境

- **開発環境**: `develop`ブランチからビルドされ、テスターグループに配布されます
- **本番環境**: `main`ブランチからビルドされ、本番ユーザーに配布されます

## コントリビューション

1. `develop`ブランチからフィーチャーブランチを作成
2. 変更を実装
3. テストを実行
4. Pull Requestを`develop`ブランチに対して作成

## ライセンス

[MIT License](LICENSE)
>>>>>>> 357d4af7f6ed0fadfedca57e9d83f86e7e87ccd6
